[프로젝트 목적]
이 프로젝트는 웹 기반 프로토타입 제작하는 것이다.
프로토타입은 실제 기능 구현이 아닌 UI/UX 흐름 검증 및 시연용이다.

---

[프로젝트 대상 시스템]

* 신규 협업 모듈: 워크스페이스 (Workspace)
* 주요 화면만 프로토타입으로 제작
* 데이터 저장, 서버 연동, 인증 기능은 포함하지 않는다

---

[입력 자료 기준]

* description.md 파일: 프로토타입 대상 주요 화면과 설명, 프로토타입 동작
* 피그마 링크: 
  * 피그마 MCP의 skill과 플러그인을 사용하여 피그마 디자인을 html로 생성
  * 최대한 resources폴더에 있는 아이콘과 이미지를 사용하여 구성
  * resources에 없는 아이콘과 이미지는 피그마에서 다운로드하여 img 폴더에 저장 후 사용

---

[개발 환경 기준]

* HTML / CSS / JavaScript 기반 정적 웹으로 구현
* 빌드 도구, 프레임워크 사용 금지 (React, Vue 등 금지)

---

[프로토타입 구현 원칙]

이 프로젝트는 “실제 기능”이 아니라 “보이는 동작” 구현이 목적이다.

다음과 같이 구현한다:

* 버튼 클릭 → 모달 / 패널 / 메뉴 표시
* 텍스트 입력 → 실제 저장 없이 입력된 것처럼 유지
* 드래그앤드롭 → 화면 위치만 변경 (데이터 변경 없음)
* 탭 클릭 → 화면 일부 전환 (페이지 이동 없음)
* 마우스 호버 → 툴팁 또는 버튼 표시
* 호버 후 버튼 클릭 → 컨텍스트 메뉴 표시

---

[금지 사항]

다음은 절대 하지 않는다:

* API 연동
* 데이터베이스 처리
* 로그인 / 인증 구현
* 복잡한 상태 관리
* 성능 최적화

---

[UI 구현 기준]

* Figma 디자인을 최대한 동일하게 구현
* 더미 데이터 사용
* 빈 화면이 나오지 않도록 샘플 데이터 포함

---

[작업 순서]

1. HTML 구조 작성
2. CSS 스타일 적용
3. JavaScript 인터랙션 추가

이 순서를 반드시 유지한다

---

[배포 및 공유 기준]

완성된 프로토타입은 URL 형태로 외부 사용자에게 공유한다

배포 방식:

* GitHub Pages 또는 Netlify 사용

배포 조건:

* index.html 필수
* 모든 경로는 상대경로 사용
* 로그인 없이 접근 가능

---

[배포 전 체크 기준]

* 버튼 클릭 시 반응이 있어야 한다
* 화면 전환이 자연스럽게 동작해야 한다
* 드래그앤드롭 동작 확인
* 입력창 입력 가능
* 주요 브라우저에서 정상 동작

---

[보안 및 주의사항]

다음 정보는 포함하지 않는다:

* 실제 서버 주소
* API 키
* 인증 정보

---

[데모 기준]

이 프로토타입은 “사용자가 클릭하며 체험하는 데모”이다

* 클릭 → 즉시 반응
* 흐름이 끊기지 않아야 함
* 실제처럼 보이는 것이 가장 중요

---

[추가 UX 권장사항]

* 초기 로딩 화면 (1~2초)
* 가이드 툴팁
* 샘플 데이터 자동 표시

---

[최종 정의]

이 프로젝트는 개발 프로젝트가 아니라
“URL로 공유되는 인터랙션 데모 사이트 제작”이다
